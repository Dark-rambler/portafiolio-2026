# Portfolio (Next.js + Tailwind)

Base portfolio project using a scalable `feature-first` architecture.

## Commands

```bash
npm run dev
npm run build
npm run lint
```

## Architecture

Main conventions are documented in `docs/architecture.md`.

Top-level source structure:

```txt
src/
  app/
  features/
  shared/
```
